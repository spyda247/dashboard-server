name: CI/CD Pipeline

on:
  push:
    branches: -main
jobs:
  build:
    runs-on: ubuntu-latest
    container: node:16
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: vercel dev
